<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.instagram.notification.model.mapper.NotificationMapper">

    <insert id="insertNotification" parameterType="Notification"
            useGeneratedKeys="true" keyProperty="notificationId" keyColumn="notification_id">
        INSERT INTO notifications (notification_sender_id, notification_receiver_id, notification_type, notification_content_id)
        VALUES (#{notificationSenderId}, #{notificationReceiverId}, #{notificationType}, #{notification_content_id})
    </insert>

    <select id="selectMyNotifications" resultType="Notification" parameterType="int">
        SELECT *
        FROM notification_detail_view
        WHERE notification_receiver_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <update id="updateReadStatus" parameterType="Notification">
        UPDATE notifications
        SET is_read = TRUE
        WHERE notification_id = #{notificationId}
        AND notification_receiver_id = #{notificationReceiverId}
    </update>
</mapper>

